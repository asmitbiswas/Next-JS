
export default function RootLayout({ children }) {
  return (
      <>
       <header style={{background:"Black", color:"White"}}>Header(Marketing2)</header>
        {children}
        <footer style={{background:"Black", color:"White"}}>Footer(Marketing2)</footer>
        </>
  );
}
